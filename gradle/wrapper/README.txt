This directory contains Gradle wrapper configuration. If you need the wrapper executable files (gradle-wrapper.jar and gradlew), run the following in a machine with Gradle installed:

On Windows PowerShell:

gradle wrapper --gradle-version 8.3.3

This will generate gradlew, gradlew.bat and the gradle/wrapper/gradle-wrapper.jar file.
